# 알로소(Alloso) 소재 제작 규칙

이 문서는 "무엇을 만들지"에 대한 브랜드 규칙만 담고 있다. 파이프라인이 "어떻게 동작하는지"(API 호출,
이미지 크롭 알고리즘, Figma 렌더링 방식 등)는 [ARCHITECTURE.md](ARCHITECTURE.md)를 참고할 것.

각 규칙 항목마다 "수정 위치"를 표시해뒀다 — 규칙을 바꾸고 싶을 때 어느 파일의 어느 부분을 고쳐야 하는지
바로 찾을 수 있도록.

> **이 문서는 이제 코드가 직접 읽는 source of truth다.** `vision/rules.js`는 이 문서를 `fs.readFileSync`로
> 읽어서 파싱한 텍스트를 그대로 Gemini 프롬프트에 사용한다(더 이상 규칙 텍스트를 `rules.js` 안에 손으로
> 옮겨 적지 않음). 즉 이 문서의 문구를 고치면 다음 실행부터 곧바로 Gemini 프롬프트에 반영된다. 숫자/경로
> 값(출력 사이즈, 폰트, 자간, LIVE 인라인 로고 비율 등)은 이 문서가 아니라 프로젝트 루트의
> `brand.config.json`에서 관리한다.

---

## 0. 소재 카테고리

알로소 소재는 4개 카테고리로 나뉜다: **네이버기획전 / 29cm기획전 / 제품인지 / 별도기획전**.
아래 1~10번 규칙은 4개 카테고리 모두에 공통 적용되는 규칙(`COMMON_RULES`)이고, 카테고리마다 달라지는
톤/레이아웃/문구 패턴은 이 문서 뒤쪽의 "카테고리별 규칙" 섹션에 카테고리명 소제목으로 채워나간다.

- 수정 위치: 이 문서의 "카테고리별 규칙" 섹션(카테고리명 소제목 추가/수정)
- `vision/analyze.js`의 `analyze()` / `buildMaterialSpec()`가 `category` 인자를 받으면
  `getProductionRules(category)`가 공통 규칙 + 해당 카테고리 규칙을 합쳐서 Gemini에 전달한다.
  CLI 사용 시 마지막 인자(또는 `--args-file`의 `category` 필드)로 카테고리명을 넘기면 된다.

**카테고리 자동 판별**: `category`를 사람이 직접 지정하지 않고 `referenceLibraryDir`(카테고리별 대표
시안을 모아둔 `레퍼런스/` 폴더)를 넘기면, `vision/analyze.js`의 `classifyCategory()`가 새 시안을 보고
`레퍼런스/` 하위 폴더 목록 중 어느 카테고리와 스타일이 가장 유사한지 Gemini로 자동 판별한다. 하위 폴더
이름이 곧 카테고리 후보이므로(하드코딩된 목록이 아님) `레퍼런스/`에 새 폴더를 추가하면 새 카테고리가
자동으로 후보에 포함된다. 확실히 유사한 카테고리가 없으면(완전히 새로운 유형) 판별 결과가 없는 것으로
처리되고, 이 경우 `getProductionRules()`가 에러 없이 공통 규칙(1~10번)만 적용해서 소재를 생성한다 —
아래 "카테고리별 규칙" 섹션에 아직 소제목이 없는 카테고리도 마찬가지로 공통 규칙만 적용된다. `category`를
사람이 명시하면 항상 그 값이 우선하고 자동 판별은 실행되지 않는다.

> **운영 가이드**: `레퍼런스/` 폴더에는 카테고리당 최소 1~2장의 `.jpg`/`.png` 대표 이미지를 넣어둘 것.
> 자동 판별은 `.jpg`/`.jpeg`/`.png`/`.webp` 파일만 대표 샘플로 인식하며, **SVG 파일은 인식하지 않는다** —
> 카테고리 폴더에 SVG만 있거나 폴더가 비어 있으면 이름만으로 후보에 오르고 시각적 근거 없이 판별에
> 참여해 정확도가 떨어질 수 있다.

**참고**: `제품인지` 카테고리는 한 장짜리가 아니라 여러 장(표지 1장 + 컬러/디테일 컷 여러 장)으로 구성된
슬라이드 세트다. 표지와 이후 슬라이드는 스타일이 완전히 다르므로(표지=큰 제품명 타이포, 이후=미니멀 태그),
슬라이드마다 `analyze.js`를 따로 실행해서 프레임을 각각 만들어야 한다. 자세한 내용은 아래 "카테고리별
규칙 → 제품인지" 참고. 정사각형 외 세로형(스토리) 버전도 함께 제작되는 카테고리지만 세로형 파이프라인
지원은 아직 없음(필요 시 나중에 추가).

---

<!-- RULES_JS:COMMON_RULES:START -->
## 1. 기본 출력 사이즈

모든 소재의 기본 출력 사이즈는 **1080 x 1080px** (정사각형).

- 수정 위치: `brand.config.json` → `outputSize`

## 2. 폰트

| 문자 종류 | 폰트 | 자간(letter spacing) |
|---|---|---|
| 국문 | Pretendard | -1.5% |
| 숫자 | Pretendard | -1.5% |
| 영문(A-Z, a-z) | Century Gothic | -2.5% |

- 한 문장 안에 국문/영문/숫자가 섞여 있으면 문자 단위로 폰트가 자동 분기된다 (예: "7.18(토) 오전 11시"에서
  "7", "18", "토", "오전", "11", "시"는 Pretendard, "(", ")" 등 기호는 국문 그룹으로 처리).
- **폰트 굵기(weight)**: 메인 타이틀은 Bold, 서브 텍스트는 Regular. Vision 분석 단계에서 시안을 보고 판단.
- Regular(얇은) 텍스트는 시안에서도 가늘고 심플한 인상을 유지해야 함.
- 지정 폰트가 Figma 파일에 설치되어 있지 않으면 자동으로 Inter로 대체된다.

- 수정 위치:
  - 폰트 패밀리/자간 값 → `brand.config.json`의 `font.krFamily`, `font.latinFamily`,
    `font.krLetterSpacingPercent`, `font.latinLetterSpacingPercent` (빌드 시 `code.ts` 상단 블록에 자동
    반영됨). 국문/영문 분기 로직 자체(어떤 문자가 어느 그룹인지 판단하는 코드)는 `code.ts`의 `isLatinChar`
  - Vision이 굵기를 판단하는 기준 문구 → 이 문서의 위 내용을 직접 수정

## 3. 텍스트 크기/정렬

- 메인 타이틀: 68px 이내 권장 (고정값 아님, 시안 비율을 보고 판단)
- 서브타이틀: 메인 타이틀의 약 60%
- 정렬(좌/중앙/우): 시안에 보이는 정렬을 그대로 따름

- 수정 위치: 이 문서의 위 내용을 직접 수정

## 4. "LIVE" 단어 처리 — 항상 로고 이미지로 자동 치환

문장 안에 "LIVE"라는 단어가 등장하면, 텍스트로 렌더링하지 않고 **저장된 LIVE 로고 PNG 이미지로 자동
치환**해서 삽입한다. (예: "가격인상 전 LIVE 단독혜택" → "가격인상 전 [LIVE 로고 이미지] 단독혜택")

- Vision 분석 단계는 그냥 "LIVE"라는 글자를 텍스트 그대로 포함시키기만 하면 되고, 실제 이미지 치환은
  Figma 플러그인이 렌더링할 때 자동으로 처리한다.
- 이 동작은 `liveBadge` 에셋(아래 6번 항목)이 스펙에 있어야 작동한다.
- 인라인 로고의 높이 = 폰트 크기 × 1.05, 로고와 텍스트 사이 여백 = 폰트 크기 × 0.15 (비율로 조정됨).

- 수정 위치:
  - 치환 크기/여백 비율 → `brand.config.json`의 `liveInlineLogo.heightRatio`, `liveInlineLogo.gapRatio`
    (빌드 시 `code.ts` 규칙 블록에 자동 반영됨)
  - Vision에게 "LIVE는 그대로 텍스트에 남겨라"라고 지시하는 문구 → 이 문서의 위 내용 및 아래 6번 항목을
    직접 수정

## 5. 로고 배치

브랜드 로고(예: `알로소 로고_화이트.png`)는 시안에서 크롭하지 않고, 별도로 제공된 실제 PNG 파일을
그대로 사용한다. Vision 분석은 로고가 들어갈 위치/크기만 판단한다.

- 수정 위치: 이 문서의 위 내용을 직접 수정. 실제 로고 파일 경로는 `brand.config.json`의 `assets.logoPath`

## 6. LIVE 뱃지 (날짜 옆 단독 뱃지)

빨간 사각형 배경에 흰색 "LIVE" 글자가 있는 뱃지(예: 날짜 텍스트 옆에 붙는 뱃지)도 로고와 마찬가지로
별도 PNG 파일(`LIVE 로고.png`)을 그대로 사용한다. 시안에 이 뱃지가 안 보이면 생략.

- 수정 위치: 이 문서의 위 내용을 직접 수정. 실제 파일 경로는 `brand.config.json`의 `assets.liveBadgePath`

## 6-1. 프로모션 뱃지 (캡슐형, 할인율/이벤트명)

완전히 둥근 모서리(캡슐/알약 모양)에 할인율·이벤트명 등이 들어간 뱃지(예: `~29% + COUPON`, `브랜드데이`)도
로고/LIVE 뱃지와 마찬가지로 텍스트가 이미 포함된 완성본 PNG(예: `29cm기획전_요소.png`)를 그대로 사용한다.
`29cm기획전` 카테고리에서 자주 등장. 시안에 이런 뱃지가 안 보이면 생략.

- 스펙 필드: `badgePlacement`(분석 결과) → `badge`(최종 스펙, base64 포함)
- 수정 위치:
  - Vision에게 캡슐형 뱃지를 인식시키는 문구 → 이 문서의 위 내용을 직접 수정
  - 스키마 정의 → `vision/analyze.js`의 `MATERIAL_SPEC_SCHEMA.properties.badgePlacement`
  - Figma 렌더링(로고/LIVE 뱃지와 동일한 `placeImageAsset` 재사용) → `code.ts`의 `buildMaterial()` 마지막 부분
  - CLI 사용 시 `node vision/analyze.js <원본> <시안> [로고] [LIVE뱃지] [출력경로] [카테고리] [프로모션뱃지PNG경로]`
    또는 `--args-file`의 `badgePath` 필드로 전달

## 7. 텍스트 강조(highlights)

"LIVE" 외에 특정 단어만 다른 색으로 강조되어 있는 경우(예: 브랜드 포인트 컬러), 그 단어와 색을
개별 지정해서 부분 색상 적용이 가능하다.

문장 속 일부 단어 뒤에 눈에 띄는 색 박스가 깔려 있는 경우(예: "가격 인상 전" 같은 경고 문구를 감싼
빨간 둥근 사각형)도 같은 highlights 항목에 `background`(RGBA)를 추가로 지정하면 그 단어 뒤에 둥근
사각형 배경을 자동으로 그려준다. 박스 없이 글자 색만 다른 경우는 `background`를 생략하면 된다.

- 수정 위치:
  - 스키마 정의(`background`, `cornerRadius` 필드 포함) → `vision/analyze.js`의 `MATERIAL_SPEC_SCHEMA`
    → `texts.items.properties.highlights`
  - 배경 박스의 좌우/상하 여백 비율, 기본 모서리 반경 → `brand.config.json`의 `highlightBadge.paddingXRatio`,
    `highlightBadge.paddingYRatio`, `highlightBadge.cornerRadius` (빌드 시 `code.ts` 규칙 블록에 자동 반영됨)
  - 렌더링 로직(텍스트를 조각내서 배경 박스 크기를 측정하고 배치) → `code.ts`의 `renderTextWithSegments()`

## 8. 가독성 처리 (텍스트 배경 패널, backdrop)

텍스트가 배경 사진 위에 있어 가독성이 떨어질 것으로 판단되면, 텍스트 뒤에 패널을 깐다. 사진 배경뿐 아니라
흰 벽처럼 단조로운 배경 위에서도 시안에 그라데이션 패널이 보이면 채운다.

- **gradient**: 텍스트 뒤에 깔리는 그라데이션 패널
  - `angle`(도, 0~360): 그라데이션 진행 방향. 생략 시 기본값 270(수직, 아래쪽이 진하고 위로 갈수록 옅어짐).
    29cm기획전처럼 대각선 그라데이션(예: 27도)이 쓰이는 경우도 있음
  - `stops`: 정지점 2개. 생략 시 기본값(0%: 투명 → 100%: 지정 색상)으로 처음부터 끝까지 균일하게 페이드.
    일정 구간까지 solid로 유지되다 그 뒤로 페이드되는 경우(예: 45%까지 solid, 92%까지 페이드) 이걸로 표현
- **blur**: 블러 처리된 반투명 패널 (기본 블러 강도 20px)
- 패널 색상(`color`)은 두 기준 중 시안에 가까운 쪽을 사용: (1) 텍스트 주변 배경 이미지의 어두운 영역에서 추출한 값,
  또는 (2) 텍스트 색과 대비되는 색 (흰 텍스트 → 검정 계열, 검정 텍스트 → 흰색 계열). 임의의 검정색 사용은 금지
- 이미 단색/그라데이션 배경 위라 가독성 문제가 없으면 생략

- 수정 위치:
  - 기본 블러 강도 → `brand.config.json`의 `backdrop.defaultBlurRadius` (빌드 시 `code.ts` 규칙 블록에
    자동 반영됨)
  - 그라데이션 방향/정지점 렌더링 로직 → `code.ts`의 `createTextBackdrop()` (angle은 배경 그라데이션과 동일하게
    `gradientTransformFromAngle()`을 재사용)
  - 스키마 정의 → `vision/analyze.js`의 `MATERIAL_SPEC_SCHEMA.properties.texts.items.properties.backdrop`
  - 판단 기준 문구 → 이 문서의 위 내용을 직접 수정 (29cm기획전 관련 보강은 아래 "카테고리별 규칙 →
    29cm기획전"에 있음)

## 9. 이미지 크롭 매칭

원본(크롭 안 된 고해상도 사진)에서 정사각형 영역을 찾아 크롭한다. 최우선 기준은 **제품이 잘리지 않고
중앙에 안정적으로 들어오는 것**이며, 시안의 구도(줌 정도, 여백 느낌)는 참고용일 뿐이다. 원본이 시안보다
훨씬 넓은 화각일 수 있어서 시안 구도를 억지로 재현하려 하면 제품이 한쪽으로 쏠리거나 잘릴 수 있으므로,
그런 경우 "제품이 안 잘리고 중앙에 오는 것"을 우선한다. 원본에서 시안 속 피사체와 동일한 피사체를 먼저
찾은 뒤 그 피사체를 중심으로 크롭 영역을 잡는다.

- 수정 위치: 이 문서의 위 내용을 직접 수정

## 10. 톤앤무드

깔끔하고 절제된 톤, 텍스트+이미지 위주 구성, 그리드형 정렬 선호, 과한 장식 지양.

- 수정 위치: 이 문서의 위 내용을 직접 수정
<!-- RULES_JS:COMMON_RULES:END -->

---

## 카테고리별 규칙

4개 카테고리 각각의 고유 규칙(1~10번 공통 규칙에 더해 적용됨)을 아래 소제목으로 정리한다. 아직 소제목이
없는 카테고리(자동 판별 결과가 없거나 여기 없는 새 카테고리명)는 공통 규칙(1~10번)만 적용된다.

<!-- RULES_JS:CATEGORY_RULES:START -->
### 네이버기획전

- 라이브 방송 예고/홍보 목적이 많아 문구에 방송 날짜·시간 정보가 거의 항상 포함됨 (예: "7.18(토) 오전 11시")
- 날짜·시간 텍스트는 LIVE 뱃지와 같은 줄에 나란히 배치되는 경우가 많음. 이때 두 가지 방식이 있으니 시안을 보고 구분할 것:
  (1) 문장 중간에 "LIVE"라는 단어로 자연스럽게 들어가 있으면 texts의 content에 "LIVE" 글자를 포함 (인라인 자동 치환)
  (2) 문장과 분리된 별도의 독립 뱃지(예: 날짜 텍스트 바로 옆에 붙은 뱃지)로 보이면 liveBadgePlacement로 처리
- 메인 타이틀(혜택/이벤트 문구, bold) + 서브텍스트(상세 설명 또는 날짜·시간 안내, regular)의 2단 텍스트 구조가 일반적
- 배경은 실제 라이프스타일 인테리어 사진(거실, 야외 정원 등)을 풀블리드로 사용, 텍스트는 흰색 위주로 사진 위에 직접 배치
- 실사용자 후기/UGC 사진을 활용한 시안에는 "@계정명" 형태의 작은 크레딧 텍스트가 하단에 별도로 붙기도 함 — 시안에 보이면 별도 text 요소로 포함할 것
- 로고는 하단 중앙 배치가 일반적이나, 시안에 보이는 실제 위치를 우선할 것

### 29cm기획전

- 텍스트는 흰색이 아니라 검정(다크) 컬러가 기본. 네이버기획전과 반대이니 혼동하지 말 것
- 로고는 "Alloso" 단독이 아니라 "Alloso X 29CM" 공동 브랜딩 마크이며, 보통 우상단 또는 좌상단에 작게 배치됨
- 할인율을 "29%"로 맞춘 문구가 자주 등장 (29CM 숫자에 맞춘 것). 검정 배경+흰 텍스트의 완전히 둥근 캡슐(pill) 모양 프로모션 뱃지(예: "~29% + COUPON", "~29% + GIFT", "브랜드데이")로 강조되는 경우가 많음 — 이 뱃지가 시안에 보이면 badgePlacement로 위치/크기만 산출할 것 (텍스트나 배경을 직접 만들지 않음)
- LIVE 방송 예고 요소(날짜·시간, LIVE 뱃지)는 이 카테고리에는 없음 — 할인/이벤트 홍보가 목적
- 메인 타이틀(제품명 또는 캠페인명, bold) + 서브텍스트(할인/이벤트 설명, regular)의 2단 구조가 기본이며, 프로모션 뱃지가 있으면 3단째로 추가됨
- 실사용자 후기/UGC 사진을 활용한 시안에는 "@계정명" 형태의 작은 크레딧 텍스트가 하단에 별도로 붙기도 함 — 네이버기획전과 동일한 패턴
- 텍스트가 검정색 기본이다 보니, 밝은 벽/창문/블라인드처럼 단조롭고 밝은 배경 위에서도 가독성 보정용 backdrop이 자주 쓰임. 이 경우 backdrop 색은 배경의 어두운 영역이 아니라 텍스트와 대비되는 흰색 계열로 판단할 것. 각도가 대각(예: 27도)인 경우가 특히 이 카테고리에 많으니 시안을 잘 보고 판단할 것

### 제품인지

- 이 카테고리는 한 장짜리 소재가 아니라 여러 장으로 구성된 슬라이드 세트다. 슬라이드 순서에 따라 스타일이 달라지니, 지금 작업 중인 시안이 표지(1번 슬라이드)인지 이후 슬라이드인지 먼저 확인할 것:
  (1) 표지(1번 슬라이드) = 히어로 타이포형: 서브카피(2줄, 작은 텍스트) + 제품명(영문, 매우 큰 사이즈로 캔버스 폭을 거의 채울 정도) + 하단 중앙의 "Alloso" 텍스트로 구성. 제품명 fontSize는 [텍스트] 섹션의 "68px 이내 권장" 가이드를 넘어설 수 있음 — 이 슬라이드는 제품명이 소재의 주인공이므로 시안 비율 그대로 크게 판단할 것. "Alloso"는 고정 화이트 로고 PNG(logoPlacement)를 쓰지 말고 일반 text 요소로 만들 것 — 색상은 그 소재의 서브카피/제품명과 같은 액센트 컬러로 지정(디자인마다 다름 — 오렌지, 브라운 등)
  (2) 표지 이후 슬라이드 = 미니멀 태그형: 마케팅 카피 없이 "●제품명" 또는 "●컬러명" 형태의 작은 텍스트 하나만 제품 옆에 배치. "●" 기호를 텍스트 앞에 그대로 포함할 것(예: "● Iceblue"). 텍스트 색은 흰색/검정 고정이 아니라 그 제품·컬러 자체에 어울리는 색으로 판단(예: 아이스블루 소파 옆에는 옅은 하늘색 텍스트). 로고, 서브카피 없음. 같은 제품의 여러 컬러/디테일 컷을 슬라이드별로 한 장씩 반복 제작하는 구조
- 로고 PNG(logoPlacement)는 이 카테고리 어디에도 등장하지 않음 — 화이트 로고 PNG를 써야 하는 시안이 명확히 보이는 경우가 아니면 생략할 것
- (참고) 이 카테고리는 정사각형과 함께 세로형(스토리) 버전도 같이 제작되지만, 현재 파이프라인은 정사각형만 지원함 — 세로형은 나중에 필요할 때 추가 예정
- 슬라이드 세트이므로, 시안을 여러 장 전달받으면 각 장을 별도 analyze.js 실행으로 처리해서 슬라이드마다 별도 프레임을 만들 것 (한 프레임에 몰아넣지 않음)

### 별도기획전
<!-- RULES_JS:CATEGORY_RULES:END -->

---

## 브랜드 에셋 파일 관례

현재 작업 폴더(`C:\Users\gram\Documents\ai 소재 제작`)는 아래처럼 구성되어 있다:

```
ai 소재 제작/
├── 원본이미지/     ← 크롭 안 된 고해상도 원본 사진들
├── 요소/           ← 브랜드 에셋 (로고 PNG, LIVE 로고 PNG, 카테고리별 프로모션 뱃지 PNG 등)
├── 네이버기획전/    ← 네이버기획전 카테고리 레퍼런스 소재
└── 29cm기획전/      ← 29cm기획전 카테고리 레퍼런스 소재
```

새 소재를 만들 때마다 필요한 것:
1. 원본 이미지 (원본이미지/ 폴더)
2. 광고주 시안 (레이아웃 레퍼런스 이미지, 보통 스크린샷)
3. `요소/` 폴더의 로고/LIVE 로고 PNG (보통 고정, 매번 새로 줄 필요 없음)
4. (`29cm기획전`처럼 캡슐형 프로모션 뱃지가 쓰이는 카테고리라면) 해당 뱃지 PNG — 뱃지 문구가 소재마다 다르면
   그때그때 새로 잘라서 `요소/`에 추가해야 함 (로고/LIVE 뱃지처럼 고정 재사용이 아님)

> 위 폴더명/파일명 관례는 `brand.config.json`의 `assets`/`paths` 필드에도 구조화되어 참고용으로 정리돼
> 있다 (참고용 문서화일 뿐, 아직 코드가 이 값을 읽어 기본 경로를 자동으로 채워주지는 않음 — CLI 인자/
> `--args-file`로 실제 경로를 매번 넘겨야 하는 점은 동일).
